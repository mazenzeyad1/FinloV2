// ── pages/dashboard/DashboardPage.tsx ─────────────────────────────────────────
export function DashboardPage() {
  return (
    <div>
      <h1 className="text-[20px] font-semibold text-text-1 mb-1">Dashboard</h1>
      <p className="text-[13px] text-text-2">Your financial overview — coming in Phase 2.</p>
    </div>
  )
}

// ── pages/transactions/TransactionsPage.tsx ───────────────────────────────────
export function TransactionsPage() {
  return (
    <div>
      <h1 className="text-[20px] font-semibold text-text-1 mb-1">Transactions</h1>
      <p className="text-[13px] text-text-2">All your transactions — coming in Phase 2.</p>
    </div>
  )
}

// ── pages/budgets/BudgetsPage.tsx ─────────────────────────────────────────────
export function BudgetsPage() {
  return (
    <div>
      <h1 className="text-[20px] font-semibold text-text-1 mb-1">Budgets</h1>
      <p className="text-[13px] text-text-2">Monthly budget tracking — coming in Phase 2.</p>
    </div>
  )
}

// ── pages/goals/GoalsPage.tsx ─────────────────────────────────────────────────
export function GoalsPage() {
  return (
    <div>
      <h1 className="text-[20px] font-semibold text-text-1 mb-1">Goals</h1>
      <p className="text-[13px] text-text-2">Savings goals — coming in Phase 2.</p>
    </div>
  )
}

// ── pages/transfer/TransferPage.tsx ───────────────────────────────────────────
export function TransferPage() {
  return (
    <div>
      <h1 className="text-[20px] font-semibold text-text-1 mb-1">Transfer</h1>
      <p className="text-[13px] text-text-2">Send & request money — coming in Phase 2.</p>
    </div>
  )
}

// ── pages/investments/InvestmentsPage.tsx ─────────────────────────────────────
export function InvestmentsPage() {
  return (
    <div>
      <h1 className="text-[20px] font-semibold text-text-1 mb-1">Investments</h1>
      <p className="text-[13px] text-text-2">Portfolio overview — coming in Phase 2.</p>
    </div>
  )
}

// ── pages/accounts/AccountsPage.tsx ──────────────────────────────────────────
export function AccountsPage() {
  return (
    <div>
      <h1 className="text-[20px] font-semibold text-text-1 mb-1">Accounts</h1>
      <p className="text-[13px] text-text-2">Linked bank accounts — coming in Phase 2.</p>
    </div>
  )
}
